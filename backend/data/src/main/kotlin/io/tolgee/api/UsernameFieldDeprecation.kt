package io.tolgee.api

const val USERNAME_FIELD_DEPRECATION =
  "Deprecated: always empty. A user's username (their e-mail) is returned only on member and admin " +
    "listings and on the user's own account, never where a user is merely referenced (author, actor, key owner)."
