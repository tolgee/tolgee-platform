import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import { Callout } from '../../../.storybook/docs/Callout';
import * as TextFieldStories from './stories';

<Meta of={TextFieldStories} name="Findings" />

# TextField — findings

Measured by hand on 2026-08-18 across 100 call sites. A work list: **done when it is empty.**

## The label above the field is not attached to it

<Canvas of={TextFieldStories.FindingNoAccessibleName} />

`common/TextField` renders the label and the input as siblings:

```
<StyledContainer>
  {label && <StyledInputLabel>{label}</StyledInputLabel>}
  <MUITextField variant="outlined" size="small" … />
</StyledContainer>
```

Nothing connects them. The `<label>` has no `for`, the input gets an id nobody points at, and no
`aria-labelledby` is set. Read off this page's own DOM:

| Shape                          | Accessible name |
| ------------------------------ | --------------- |
| **label above — the standard** | **none**        |
| floating label                 | "Task name"     |
| underline                      | "Task name"     |

**The two shapes this page calls defects are the two that name the field.** Material UI wires
`label` to the input itself; the wrapper took the label away from Material UI and never rewired it.

It costs more than the announcement: a `<label>` without `for` is not clickable either, so the
label above a Tolgee field does not focus it the way a label is supposed to.

`common/Select` copies the same block from `common/TextField`, so every wrapper select is in the
same state — see **Select → Findings**.

<Callout title="Suggested fix" kind="warning">
  Both wrappers own the label and the control, so this is two files: take an
  `id` (`useId()` when the caller passes none), put it on the field and
  `htmlFor` on the label. For `Select`, give `InputLabel` an `id` and pass it as
  `labelId` — that is the only thing Material UI reads. Around 74 call sites are
  named by the change without one of them being touched.
</Callout>

## One field, four shapes

<Canvas of={TextFieldStories.FindingThreeShapes} />

Every one of the 100 call sites, classified by where its `TextField` comes from and what it renders:

| Shape                                | Call sites |                               |
| ------------------------------------ | ---------- | ----------------------------- |
| **label above an outlined box**      | **53**     | the standard                  |
| no label, through the wrapper        | 10         | fine — search boxes and cells |
| no label, straight from Material UI  | 15         | fine, but outside the wrapper |
| **underline** (`variant="standard"`) | **21**     | not the product's field       |
| **floating label**                   | **1**      | not the product's field       |

So 63 fields go through the wrapper and come out as an outlined box; 22 do not.

<Callout title="Suggested fix">
  The 21 underlined fields first — they are concentrated in eleven files and
  listed on the Inventory page. The single floating label after that.
</Callout>

## The wrapper states the standard but does not hold it

<Canvas of={TextFieldStories.FindingOverridable} />

`common/TextField` sets `variant="outlined"` and `size="small"` — and then spreads the caller's
props after them:

```
<MUITextField ref={ref} variant="outlined" size="small" sx={…} {...otherProps} />
```

So any caller can walk over the decision with one prop, and **eleven formik fields do exactly
that**, rendering as `standard` through a wrapper whose whole point was to stop that.

<Callout title="Suggested fix">
  Spread `otherProps` first and set `variant` after it, so the wrapper's own
  decisions win. Then fix the eleven callers that were relying on the override.
</Callout>

## A typo nobody sees, in two places

`tg.component/common/TextField` styles its label with:

```
font-size: 14px;
font-weight: 500px;
```

`500px` is not a font weight. The browser drops the declaration, so the label renders at the default
400 rather than the intended medium.

It does not stop at TextField. `common/Select` imports the very same `StyledInputLabel`, and both
wrappers are in turn wrapped by their formik versions — so the broken label reaches **66 files**:

| Through                 | Files |
| ----------------------- | ----- |
| `common/TextField`      | 15    |
| `form/fields/TextField` | 39    |
| `common/Select`         | 8     |
| `form/fields/Select`    | 4     |

`common/Switch` has its own copy of the same mistake, `font-weight: 400px`. That one happens to be
invisible — the fallback is 400 anyway — which is exactly why it survived.

<Callout title="Suggested fix">
  `font-weight: 500` in the shared label and `400` in Switch. Expect the labels
  to change weight where they should have been medium all along.
</Callout>

## The theme declares a root font size nothing sets

`htmlFontSize: 15` tells Material UI that one `rem` is 15px, and every conversion it makes between
px and rem is based on that number. Nothing sets the root to 15px though — `MuiCssBaseline` styles
`html` for height only, and puts `fontSize: 15` on `body`. The root stays at the browser's 16px.

The typography scale is written in px, so most of it is unaffected. What is affected is anything
Material UI expresses in rem on our behalf, which comes out about 7% larger than the theme intends.

<Callout title="Suggested fix">
  Either set `html { font-size: 15px }` in `MuiCssBaseline` so the declaration becomes true, or drop
  `htmlFontSize` and let it be 16. Both are one line; leaving them disagreeing is the only wrong
  option.
</Callout>

## Five search fields, one of them shared

`common/form/fields/SearchField` is the real one: magnifier, clear button, 500ms debounce. Four
others exist beside it.

| Field                     | Built on                                        |
| ------------------------- | ----------------------------------------------- |
| `SearchField`             | `TextField` — the shared one                    |
| `SecondaryBarSearchField` | wraps `SearchField` ✓                           |
| `HeaderSearchField`       | its own `TextField`, adornments assembled again |
| `FlagSearchField`         | its own                                         |
| `TranslationsSearchField` | its own                                         |

`HeaderSearchField` repeats the adornments, the clear button and the icon sizes that `SearchField`
already has, and the two now differ in ways nobody chose.

<Callout title="Suggested fix">
  Fold `HeaderSearchField` onto `SearchField` the way the secondary bar already
  does, and check whether the other two can follow.
</Callout>

## Two search fields import icons from the package

`SearchField` and `HeaderSearchField` both do:

```
import { SearchSm, XClose } from '@untitled-ui/icons-react';
```

**Foundations → Icons** asks for the library instead. These two are the closest violations to hand
because they sit in shared components, where the next person copies from.

<Callout title="Suggested fix">
  Import from `@tginternal/library/icons`, and add the eslint rule that makes
  the page's own instruction enforceable.
</Callout>

## Not wrong

75 of the 100 pass a `label`, and the 26 placeholders sit where the label is carried by the layout.
`helperText` doubles as the error message on all 8 error states rather than adding a second line, so
the form never jumps.
