// Same reasoning as sessionsGrid.ts: the action column is fixed rather than `auto`, because `auto`
// resolves to the button's width in a row but to zero in the header's empty cell.
export const CONNECTED_APPS_GRID_COLUMNS = '1.6fr 1.8fr 1.4fr 1.2fr 1.2fr 96px';

export const CONNECTED_APPS_ROW_PADDING = '8px 12px';

export const CONNECTED_APPS_WIDE_LAYOUT = '760px';
