import { Typography } from '@mui/material';
import { T } from '@tolgee/react';

export const PaidFeatureBanner = () => {
  return (
    <Typography>
      <T keyName="permissions_advanced_paid_banner" />
    </Typography>
  );
};
