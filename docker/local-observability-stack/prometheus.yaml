# =============================================================================
# LOCAL DEVELOPMENT ONLY - Do not use in production.
# =============================================================================
#
# Prometheus - Metrics Storage
#
# Purpose:
#   Stores metrics generated by Tempo's metrics generator, enabling time-series
#   analysis of span durations, request rates, and error rates.
#
# Part of: Tolgee Local Observability Stack
#   See: docs/observability/
#
# =============================================================================

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
