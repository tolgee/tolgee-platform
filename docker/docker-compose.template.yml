services:
  db:
    image: postgres:18.0
    environment:
      - POSTGRES_PASSWORD=postgres
  app:
    image: tolgee/tolgee
    ports:
      - "8090:8080"
    env_file:
      - .env
    depends_on:
      - "db"
