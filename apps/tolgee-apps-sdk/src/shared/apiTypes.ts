/**
 * Tolgee's generated REST schema, re-exported so app code can name a response
 * shape (`TolgeeApiSchemas['ProjectActivityModel']`) without importing
 * `@tginternal/client` itself — the same reason `TolgeeAppClient` is aliased.
 */
import type { components } from '@tginternal/client'

export type TolgeeApiSchemas = components['schemas']
